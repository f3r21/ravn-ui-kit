# UI-Kit Master Plan

Source of truth for chunked, checkpointed work on this Storybook UI-Kit. Target repo: `ravn-ui-kit` (this repo). Ground truth: `/Users/99/Developer/RAVN/RAVN/Coding Challenge/UI-Kit`. Reference styles worth reusing (not authoritative, but reliable): `/Users/99/Developer/RAVN/ravn-task-management-challenge/src/styles/tokens.css`.

Scope: full ground-up re-audit of every component against the ground-truth Figma exports, even where prior commits already touched a component — nothing is treated as pre-verified.

Workflow per chunk (see project CLAUDE.md-equivalent instructions):
1. Pull exact spec values for the chunk's component(s) from the ground-truth spec file(s); diff against current implementation.
2. Implement fixes directly in component + story files (CSF3 object syntax, co-located files, exhaustive variants).
3. Run `npm run typecheck` (and relevant tests).
4. Check off the item below.
5. `git add .` + `git commit -m "Completed Chunk N: <name>"`.
6. `/compact` or `/clear` before starting the next chunk.

## Known ground-truth facts (already established, do not re-derive)

- **Colors**: `neutral.1-5`, `primary.1-4`, `secondary.1-4`, `tertiary.1-4`, `success`, `warning.1-6`, `danger.3-6`, `transparent.light/dark` (6 alpha steps each) match `src/styles/theme.css` hex-for-hex already. One outlier not yet tokenized: standalone `Blue #2F61BF` (used in Tags).
- **Radii**: ground truth uses literal per-component values (4px pills/tags/datepicker-dropdown, 8px buttons/cards, 16-20px containers, 24px sidebar-inner, 40px guideline-frame), not a clean ramp. Current scale (`sm 8/md 16/lg 24/xl 40`) is **missing a 4px step** — root cause of the already-flagged `task-column.tsx` 20px-literal issue; likely affects Tags/Datepicker too.
- **Typography**: Desktop body/UI text is `SF Pro Display`/`SF Pro Text` (current `--font-sans`, fixed in a prior commit). **Display-style headings use `DM Sans`** — no token yet. Mobile variants (Roboto/Android, SF Pro/iOS) are out of scope for a web Storybook; note only.
- **Shadows**: no tokens exist yet for: Drop Shadow Large (`0 0 72px rgba(0,0,0,.04)`), Drop Shadow Small (`0 0 16px rgba(0,0,0,.04)`), Datepicker elevation (3-layer), Top Nav hard-edge (`0 2px 0 #2C2F33`).

## Chunks

### Foundation
- [x] Chunk 1: Design Tokens audit. Colors: added `--color-blue: #2f61bf` (Tags-only accent, not part of a core ramp). Radii: confirmed sm(8)/md(16)/lg(24) are real (buttons, search bar, sidebar-inner respectively); added `--radius-2` (2px, datepicker day cell), `--radius-4` (4px, tags/pills — most common radius in the kit), `--radius-10` (10px, segmented-control track), `--radius-20` (20px, task-column/sidebar outer frame); flagged `--radius-xl` (40px) as likely fabricated — its only ground-truth occurrence is a 40×40 circular button (`rounded-full`), not a real distinct token; its 3 current consumers need re-checking in Chunks 11/14/15. Typography: re-verified `--font-sans: 'SF Pro Display'` is correct — DM Sans only appears in doc-table specimen labels and in `Date Picker.md`, which (per the existing typography.mdx note) doesn't correspond to any shipped component; no `--font-display` token added. Shadows: added `--shadow-small` (switch pill), `--shadow-elevation` (datepicker dropdown), `--shadow-nav` (top-nav hard edge); "Drop Shadow Large" intentionally excluded — only appears on the Figma documentation page frame, never a real component. Updated `colors.mdx` (removed stale "pending verification" banner, added Blue swatch, radius/shadow tables). `typography.mdx` needed no changes. `npm run typecheck` passes.

### Primitives
- [x] Chunk 2: Avatar + UserRow — 3 sizes (32/40/48px). Source: `Avatar00/01.md`. Sizes 32/40/48 (sm/md/lg) already matched the Figma "Avatar" Property 1=Default/Variant2/Variant3 variants exactly, no change needed. Found and fixed two real mismatches: (1) `avatar.tsx` had a fabricated `border border-neutral-2` — every real Avatar instance in the ground truth (`Ellipse 93`) has no border; the only bordered ellipse in the export (`Ellipse 90`, 2px solid white) belongs to an unrelated "Circle Actions Placeholder" element, not the Avatar component — removed. (2) `user-row.tsx` name text used `font-semibold` at a per-size scale (`text-xs/text-sm/text-base`), but every "User" component instance across the ground truth (Task Column, Cards, ApplicationSidebar footer) renders the name identically as Desktop/Body/M/regular: SF Pro Display, 15px/24px, letter-spacing 0.75px, weight 400 — fixed to a fixed `text-[15px] leading-6 tracking-wider font-normal`, reusing the same `tracking-wider` (0.05em = 0.75px @ 15px) trick already established in `search-bar.tsx` for this identical spec token. Name text is now constant across the `size` prop (no Figma instance ever pairs a different name size with a different avatar size); `role` (unspec'd — no ground-truth instance shows a subtitle line) keeps its own per-size scale unchanged since nothing contradicts it. `isOnline` status dot is likewise a spec-free addition, left as-is since it's opt-in and never contradicts a real instance. `npm run typecheck` and `vitest run avatar` pass.
- [x] Chunk 3: Tag + LabelCheckbox — style×icon×type matrix, 4px radius, `Blue` token wiring. Source: `Tags00/01.md`. Tag was fully fabricated (uppercase, `text-xs font-semibold tracking-wide`, per-shade bg/text/border tints) — rebuilt against the real matrix: Style=Solid (10%-alpha fill, no border) × Style=Outline (1px border, transparent fill) × Type=General/Green/Blue/Yellow/Red, using `--radius-4`/`rounded` (4px, Chunk 1) and Desktop/Body/M/bold (SF Pro Display 15px/24px, weight 600, letter-spacing 0.75px via `tracking-wider`, per the Chunk-2-established convention). Kept `variant` prop's existing key names (`primary`→red/`DA584B`, `secondary`→green/`70B252`, `tertiary`→yellow/`E5B454`, `neutral`→general/white already had correct hue mappings, just wrong chrome) and added `blue` (the Chunk-1 `--color-blue` token) as a 5th option — zero breaking changes to the 3 existing consumers (`task-card.tsx`, `task-table.tsx`, `add-task-modal.tsx`). Added `outline` (boolean) and `icon` (ReactNode, 24×24 Icon=Left slot, expects `currentColor`) props, both opt-in/non-breaking. "General" type is the one case where the solid fill tint (neutral.2) and the foreground (neutral.1/white) come from different swatches in the spec — kept as a special case rather than forcing a single-swatch model. LabelCheckbox was a fabricated 16px checkbox+separate-label pattern with no ground-truth basis; real "Label Checkbox" component (Tags01.md, Add Task Modal04/05.md) is structurally identical to the Tag Icon=Left chip (24×24 icon slot, Desktop/Body/M/regular label, 4px/16px padding, 8px gap, 4px radius, no fill/border) — rebuilt to match. Figma's Property 1=Default and Property 1=Selected export byte-for-byte identical style values (no color/bg distinction given), so checked/unchecked is carried by the icon glyph alone (empty vs. checked box path) rather than an invented background highlight; kept the existing react-aria `useCheckbox` behavior/API intact since it isn't contradicted by anything in spec and all 4 existing tests still pass unmodified. `npm run typecheck` and full `vitest run` (30/30) pass.
- [x] Chunk 4: Button (icon button) + TextButton — full state×type matrix (Default/Disable/Hover/Selected × Primary/Secondary). Source: `Button, Switch Button00/01.md`. Ground truth defines exactly two components in this frame: "Button" (icon-only, 40×40px, border-radius 8px, Property1=Primary single "Normal" state / Property1=Secondary toggling Selected↔Unselected) and "Text Button" (despite its name, a solid-fill label pill — State=Default/Disable/Hover/Selected × Type=Primary/Secondary, 8px padding all sides, border-radius 8px, Desktop/Body/M/regular). Both were previously fabricated: `button.tsx` was a generic 4-variant/3-size text button matching nothing in spec, and `text-button.tsx` was flagged in a prior chunk's comment as an underline/link style that didn't match the real solid-fill matrix it was named after — this chunk resolves that flagged ambiguity by rebuilding both against the literal Figma component names/shapes. Rebuilt `button.tsx` as the true icon button (`variant` Primary/Secondary, `isSelected` for Secondary's toggle, required `aria-label` since it's icon-only, dropped the fabricated `size`/`isLoading` props — no ground-truth basis for either). Rebuilt `text-button.tsx` as the solid pill (`variant` Primary/Secondary, `isSelected`, disabled state uses the exact spec colors per type rather than generic opacity — note Type=Primary's Disable and Hover states are literally the same `primary-2` swatch in the source, not a transcription error). Migrated the real consumers that were using the old generic `Button` for text-labeled CTAs (`add-task-modal.tsx`, `assignee-modal.tsx`, `estimate-modal.tsx`, and the `Open … modal` trigger buttons in their stories + `modal.stories.tsx`) to `TextButton` — `variant="primary"` for primary actions (Create Task/Confirm/Set points), `variant="secondary"` for Cancel — since that's what those buttons visually are per the now-corrected component split. `npm run typecheck`, full `vitest run` (30/30), and `npm run build:storybook` all pass.
- [x] Chunk 5: Switch (segmented/toggle control) — track/pill dimensions, Drop Shadow Small. Source: `Button, Switch Button00/01.md`. Re-audited both export files plus a full-directory grep of the ground-truth `UI-Kit` folder for "toggle"/"thumb"/"track": confirmed no boolean track+thumb switch exists anywhere in spec. The real "Switch button"/"Segmented Control" layers are a two-option pill selector, already implemented as `segmented-control.tsx` — it was already structurally correct (flex layout, 0 gap, 4px container padding, 4px/24px pill padding, bg-neutral-2 active fill, identical white label color both states) from a prior pass, so this chunk was a fidelity tightening rather than a rebuild: swapped the pre-Chunk-1 arbitrary `rounded-[10px]` for the real `--radius-10` token (`rounded-10`, confirmed compiling via a storybook-static build check since no standalone Tailwind CLI is installed), swapped `shadow-sm` (Tailwind default) for the correct `shadow-small` token (Chunk 1's exact `0px 0px 16px rgba(0,0,0,.04)`), and fixed the label typography from a fabricated `text-sm font-semibold` to the spec's `Desktop`-rendered IOS/Subheadline/S.regular values: `text-[13px] leading-[13px] tracking-[1px] font-normal` (kept on the shared `--font-sans` token per the Chunk 1 "no separate SF Pro Text token" precedent — mobile-labeled Figma text-style names don't get their own family). Deliberately did *not* apply `shadow-small` to the inactive pill even though the spec's CSS lists a `filter: drop-shadow` there too — that's a Figma effect on an otherwise-invisible transparent shape, and our `shadow-small` is a `box-shadow` (always draws behind the full element box regardless of transparency), so applying it would render a visible phantom rectangle never seen in the reference. Separately, re-confirmed `switch.tsx` (the fabricated boolean iOS-style toggle already flagged as unverified in an earlier pass) still has zero ground-truth basis anywhere in the full export — left unchanged as a defensible standalone pattern, comment updated to record the wider re-search. `npm run typecheck` and full `vitest run` (30/30) pass; `npm run build:storybook` used once to verify the new `rounded-10`/`shadow-small` utility classes actually compile (build artifact is gitignored, not committed).
- [x] Chunk 6: Input — verify `useTextField` label association still correct post-rebuild. Confirmed via ground-truth `CLAUDE.md` component index (Components/ directory listing) that no standalone "Input"/"Text Field" component exists anywhere in the Figma export — the only real editable-text-field instance found (`Mockups/Dashboard Add Task/Add Task Modal05.md`'s inline title field: borderless, Desktop/Body/XL/bold 20px, on a neutral.3 dark surface) belongs to the compact inline add-task widget, not a generic reusable primitive — deferred reconciling that specific look to Chunk 13 (AddTaskModal rebuild) rather than forcing it onto this shared atom now. The label-association check itself found a real bug: `input.tsx` tracked `error` as a purely visual prop and never passed `isInvalid`/`errorMessage` into `useTextField`, so react-aria's `useField` never generated an `errorMessageProps.id` — the visible error `<span>` was never actually wired to the input via `aria-describedby`/`aria-invalid`, despite looking correct visually. Fixed by passing `isInvalid: !!error, errorMessage: error` into the `useTextField` call. Found and fixed the identical copy-pasted bug in `datepicker.tsx` (same structure, same missing wiring) as a drive-by since it's the same defect class in a near-identical file — full Datepicker fidelity audit remains Chunk 16's job. Added `input.test.tsx` (4 tests: label association, `aria-invalid`/`aria-describedby` wiring when `error` is set, no `aria-invalid` when clean, `onChange` value plumbing) — previously had zero test coverage. Also translated leftover Spanish story args in `input.stories.tsx` and `datepicker.stories.tsx` to English per global no-Spanish-in-code-artifacts rule (arbitrary example copy, not ground-truth ‑derived content — left `datepicker-menu.tsx`'s literal calendar day-of-week labels alone since that's real product locale content, not incidental story text, and is Chunk 16's scope if it needs revisiting). `npm run typecheck` and full `vitest run` (34/34, up from 30/30) pass.
- [x] Chunk 7: Tabs (underline variant) — Selected/Variant2 states. Source: `Button, Switch Button00.md` tab section. Confirmed via a full-directory grep that the only real spec for this component is the "Tabs"/"Property 1=Selected"/"Property 1=Variant2" block in `00.md` (the only other hits were mobile Android mockups, out of scope per the master-plan note, and an unrelated "Swicter" icon-button pair in `01.md`). Frame 299 (per-tab box) is a flex column of two auto-layout children — the label (order 0) and a 2px "indicator" strip (order 1) — with padding 12px/0px/8px and an 8px gap; `tabs.tsx` already modeled the 12/8 vertical padding and a bottom-flush 2px indicator from a prior pass, so this chunk was a targeted fix rather than a rebuild. Found and fixed a real mismatch: the label typography was a fabricated `text-sm font-semibold` (14px/600); the spec's "Android/Body/S/regular" style is 13px/20px, weight 400, letter-spacing 0.25px, centered — fixed to `text-[13px] leading-5 font-normal tracking-[0.25px] text-center`, kept on the shared `--font-sans` token per the Chunk 1 precedent that mobile-labeled Figma text styles (Android/Roboto here, iOS/SF Pro Text elsewhere) don't get their own font-family token. Removed a fabricated `border-b border-neutral-3/50` under the whole tablist — no such full-width divider exists anywhere in spec, only the per-tab 2px indicator (which is already implemented and is the sole real state differentiator). Left the existing `px-5` horizontal padding as-is (already-documented deviation from spec's literal `0px`, which is an artifact of the demo's fixed 120px-wide box, not a real constraint for arbitrary-length labels) and left the icon slot/hover-state as spec-free, non-contradicted additions, consistent with how earlier chunks treated unspecced extras. Drive-by: translated leftover Spanish story panel copy in `tabs.stories.tsx` ("Vista Tablero (Kanban)"/"Vista Lista"/"Vista Tabla" → "Board view (Kanban)"/"List view"/"Table view") per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunk 6. `npm run typecheck`, full `vitest run` (34/34), and `npm run build:storybook` all pass.

### Composites
- [x] Chunk 8: SidebarItem — Default/Selected/Hover states, gradient, 56px row, 4px indicator bar. Source: `SideBarItem00/01.md`. Found the real, unambiguous component definition in `01.md`'s "Sidebar Tab" States=Default/Hover/Selected block (`00.md`'s in-context instances mix Android/iOS text styles for the same layers — Figma mobile-breakpoint noise, out of scope per the master-plan mobile note). `sidebar-item.tsx` had several real mismatches: (1) padding was `px-4 py-3.5` (16px all sides) instead of spec's `0 0 0 16px` (left-only, height fixed at 56px/`h-14` with vertical centering) — fixed. (2) label typography was fabricated `text-sm font-semibold` (14px); spec is Desktop/Body/M/bold — SF Pro Display, 15px/24px, weight 600, letter-spacing 0.75px — fixed to `text-[15px] leading-6 font-semibold tracking-wider` (the Chunk 2/3 `tracking-wider` convention). (3) Hover state had a fabricated `hover:bg-neutral-4/50` — the real Hover export has no `background` line at all (only Selected does); the *only* Hover→Selected difference in spec is the gradient background and the indicator's opacity, not a background tint on Hover — removed, hover now only recolors text/icon to `primary-4` (matching spec, which gives Hover the same primary.4 color as Selected). (4) icon slot was a bare `text-lg` span (~18px font-size, no real box) instead of the spec's fixed 24×24 "Icon Placeholder" — fixed to `w-6 h-6` matching the `currentColor` convention established for Tag's icon slot in Chunk 3. (5) the 4px "Rectangle 33" indicator bar was conditionally mounted only when active; spec always includes this layer across all 3 states and toggles `opacity: 0`↔`1` instead — changed to always-rendered with `opacity-0`/`opacity-100`, enabling a transition rather than a mount/unmount pop. Width (`w-1` = 4px) and full-row height were already correct. Added `aria-current="page"` when `isActive` as a non-contradicted a11y addition (same spirit as Chunk 6's aria wiring fixes). `badgeCount` has no ground-truth basis anywhere in either export but is kept as a pre-existing, non-contradicted opt-in addition per established precedent. Drive-by: translated a leftover Spanish `aria-label="Navegación principal"` → `"Main navigation"` in the sibling `application-sidebar.tsx` (Chunk 9's real scope, but same no-Spanish-in-code-artifacts class of fix as Chunks 6/7). Rewrote `sidebar-item.stories.tsx` to cover the actual state matrix: added a `Hover` story (using the project's existing `storybook-addon-pseudo-states` — `parameters: { pseudo: { hover: true } }`, first use of that addon in a story file), renamed the active story to `Selected` to match the real Figma state name, and added a placeholder icon to `Default`/`Selected` since no story previously exercised the icon slot at all. `npm run typecheck` and full `vitest run` (34/34, unchanged — no prior SidebarItem test file existed and none was required by this chunk's scope) pass; `npm run build:storybook` verifies the new `h-14`/`text-[15px]`/`tracking-wider` utility classes and the pseudo-states addon story compile (artifact gitignored, not committed).
- [x] Chunk 9: ApplicationSidebar — 310px/232px variants, logo, user footer. Source: `ApplicationSidebar00/01.md`. Cross-checked both doc exports against the real in-context "Sidebar" layer in `Mockups/Dashboard Default View/Dashboard Mockup.md` to resolve which is the actual desktop size. `01.md`'s Sidebar (232×836, `neutral.4` #2C2F33, `border-radius: 24px`, logo top:12px, tab list top:96px) matches the Dashboard Mockup's Sidebar layer property-for-property. `00.md`'s Sidebar (310×844, no border-radius, logo top:36px, tab list top:120px) never appears in any real desktop screen — a grep for `width: 310px` across the full ground-truth tree found it only in `00.md` itself, `SideBarItem00.md` (already flagged in Chunk 8 as Android/iOS mobile-breakpoint noise), and `Mockups/Mobile/Android/Dashboard Default View/Sidebar.md` — confirming 310px is the mobile-Android sidebar width, not a second real desktop variant. No `size` prop added; `application-sidebar.tsx` already correctly targeted the 232px/24px-radius shape from a prior pass, so this chunk mainly documents/confirms that choice rather than changing it. Found and fixed one real mismatch: the logo container used `pt-3 pb-6` (undershoots the spec's top:96 tab-list offset by ~20px for a 40px-tall logo) — changed to a fixed `h-24` (96px) wrapper with `pt-3`, so the tab list always starts at the correct 96px offset regardless of the consumer-supplied logo's intrinsic height. Found and removed a fabricated `footer` prop (e.g. "user profile" slot): grepped the isolated component doc, the in-context Dashboard Mockup, and every other mockup file for any footer/avatar/profile layer within the Sidebar's bounds — none exists anywhere; the only Avatar near this layout belongs to the Top Nav bar (`left: 296px`, Chunk 10's scope), not the sidebar. The `footer` prop had zero ground-truth basis and zero real consumers in this codebase (only a stale doc comment in `user-row.tsx` referencing it, now corrected) — removed per the same fabricated-prop precedent Chunk 4 set for Button's unfounded `size`/`isLoading`. Drive-by: translated leftover Spanish placeholder copy ("Área de contenido principal" → "Main content area") in `application-sidebar.stories.tsx` per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunks 6-8. `npm run typecheck`, full `vitest run` (34/34, unchanged — no ApplicationSidebar test file existed and none was required by this chunk's fidelity-only scope), and `npm run build:storybook` all pass.
- [x] Chunk 10: TopNav + SearchBar — search input, avatar/notification icons, tab bar Default/Selected. Source: `Top Navigation Bar00/01.md`. Cross-checked both isolated doc exports against the real in-context "Search Bar" instance in `Mockups/Dashboard Default View/Dashboard Mockup.md` (`left: 296px` — flush against the 232px sidebar + 32px gaps — `right: 36px`, `top: 32px`, height 64px, neutral-4 bg, 16px radius), which confirms `01.md`'s "Search Bar" as the real desktop component (the isolated docs' "tab bar Default/Selected" the plan bullet referenced turned out to be `00.md`'s mobile-only "Tab Bar Element" bottom-nav — grepped and confirmed absent from every desktop mockup, out of scope per the established mobile-exclusion precedent; the real Default/Selected pair belongs to the Search Bar itself). Found the whole prior implementation was structurally fabricated: `top-nav.tsx` was a separate `<header>` with its own bg/border-b/h-16 chrome wrapping a compact, self-backgrounded `SearchBar` plus a fabricated `title` prop — but Figma's real "Search Bar" component is a single unified bar (no separate title/header layer exists anywhere in the real export) containing two sub-frames: Frame 649 (icon+text, no fill of its own, fixed 171px width with zero room for the previously-fabricated inline clear button) and Frame 648 (trailing icon(s)+avatar). Rebuilt accordingly: `search-bar.tsx` now renders only Frame 649 (transparent, `gap-6`, search icon + input, Desktop/Body/M/regular text, no self-owned background/clear-button — matches its decorator, which was already staging it on a neutral-4 backdrop rather than supplying one itself, a pre-existing tell that this was always meant to be composited); `top-nav.tsx` now renders the real outer "Search Bar" container (`bg-neutral-4 rounded-md px-6 py-3` — confirmed `--radius-md`'s Chunk-1 doc comment already cites this exact component as its 16px source) composing `SearchBar` on the left and a trailing icon/avatar slot on the right, dropped the fabricated `title`/`showSearch`/`actions` props (zero ground-truth basis, zero real consumers — same precedent as Chunk 9's footer removal). Frame 648's Default→Selected delta is a literal width jump (88px = 1 icon + avatar, to 136px = 2 icons + avatar — exactly one more 24px icon + 24px gap) with no legible second-icon glyph or label in the export and zero appearances of the Selected state in any real desktop mockup; read this ambiguous-but-structurally-real delta as a clear-search affordance (shown once there's a value to clear) as the most conservative interpretation that matches the pixel math without inventing unrelated functionality — verified the 88px/136px arithmetic holds exactly under the new `gap-6` flex layout. Confirmed `--shadow-nav` (already tokenized in Chunk 1) is mobile-only: grepped the full ground-truth tree and its one `box-shadow: 0 2px 0 #2C2F33` source is the separate mobile "Top Nav" component in `Top Navigation Bar00.md` (390px width, avatar+headline+icons layout) which never appears on the desktop Search Bar — left unapplied here, still unused in `theme.css` pending a real desktop consumer. Drive-by: translated leftover Spanish `aria-label`s (`'Buscar'`→`'Search'`, `'Limpiar búsqueda'`→ removed/replaced by `'Clear search'`) per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunks 6-9. Added `top-nav.test.tsx` (4 tests: search value plumbing, clear-icon visibility tracking the 88px/136px structural delta, clear-icon click behavior, avatar-only-when-user-provided) — previously had zero test coverage for either file. `npm run typecheck`, full `vitest run` (38/38, up from 34/34), and `npm run build:storybook` all pass.
- [x] Chunk 11: Card family — TaskCard, ProjectInfo, Reactions — timer chip, tag chips, avatar row, reaction counters. Source: `Cards00/01.md`. `Cards01.md` is the clean, consistently Desktop-labeled export (`Desktop/Body/L/bold` title, `Desktop/Body/M/bold` timer/tags) — used as primary source; `Cards00.md`'s parallel Android/Roboto-labeled Task Card instance confirmed as the same mobile-breakpoint noise pattern established in prior chunks, cross-checked rather than treated as a second desktop variant. `project-info.tsx` was fully fabricated (name/description/status-badge/progress-bar card) with a doc comment claiming a "Task Column" source that doesn't exist — grepped all 4 `Task Column0*.md` files and confirmed the real, recurring "Project Info" component (5+ instances) is always just a title (`Working (03)`, Desktop/Body/L/bold, 18px/32px/600/0.75px, flex-grow) + an optional trailing 24×24 muted-icon slot, nothing else; had zero real consumers anywhere in the codebase, so rebuilt from scratch to match and wired it into `TaskCard`'s title row (previously a duplicate hand-rolled copy of the same row). `reactions.tsx` was structurally already correct (count-before-icon ordering, `gap-4`/`gap-1` matching Frame 653's real 16px/4px auto-layout) from a prior pass, but had fabricated typography (`text-xs font-semibold`, 12px/700) against the spec's `Desktop/Body/M/regular` (15px/24px/400/0.75px) — fixed; also had zero real consumers (TaskCard reimplemented a cruder 2-counter emoji version inline) — wired in via a new `reactions: Reaction[]` prop replacing the old hardcoded `commentsCount`/`attachmentsCount` numeric props (which had no basis in spec's generic icon-placeholder pattern beyond an assumed comment/attachment semantic), migrating the 2 real consumers (`task-card.stories.tsx`, `task-column.stories.tsx`). `task-card.tsx` fixes: due-date pill was a bespoke span (`text-xs`, fabricated warning-5/danger-5 + border combo with no spec evidence) — rebuilt to render the real shared `Tag` component instead (it IS a literal "Tag" instance per spec: 4px/16px padding, 8px gap, 4px radius, alarm-line icon, `Desktop/Body/M/bold`), mapping `dueDateUrgency` onto Tag's already-verified variant palette (`normal`→neutral, `warning`→tertiary, `overdue`→primary) rather than inventing new unverified color combos; points text fixed from fabricated `text-sm font-bold` (14px/700) to spec's `text-[15px] leading-6 font-semibold tracking-wider` (15px/24px/600/0.75px). Figma's Frame 653 shows a 3rd, leading icon-only reaction slot with no count ever rendered (`display: none` on its text layer) — left unimplemented, same as the pre-existing 2-counter version's flagged ambiguity, since no legible glyph or contradiction-free semantic exists for it. Drive-by: translated leftover Spanish story copy (`'3 DÍAS'`→`'3 DAYS'`, `'VENCIDO'`→`'OVERDUE'`, `'Fernando Ramírez'`→`'Fernando Ramirez'`) in `task-card.stories.tsx` and `task-column.stories.tsx` per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunks 6-10. `npm run typecheck`, full `vitest run` (38/38, unchanged — no card-family test file existed and none was required by this chunk's fidelity-only scope), and `npm run build:storybook` all pass.
- [x] Chunk 12: Modal (base) — radius correction re-verify (`rounded-2xl`→8px). Confirmed via grep that no standalone floating-dialog "Modal" component exists anywhere in the ground truth — the only two real analogs sharing this shell's chrome are the "Assignee Modal" dropdown (`Components/Task Column01.md`, 239×432px, `background: #393D41` neutral.3, `border: 1px solid #94979A` neutral.2, `border-radius: 8px`) and the compact inline "Add Task Modal" widget (`Mockups/Dashboard Add Task/Add Task Modal00.md`, `background: #393D41` neutral.3, `border-radius: 8px`, no border) — both are absolute-positioned anchored popovers next to their trigger, not centered full-viewport dialogs with a dark backdrop scrim; that architectural mismatch (popover vs. centered-dialog) is explicitly Chunk 13's job ("cross-check against `Task Column01.md` inline modal spec"), left untouched here. The `rounded-2xl`→8px fix flagged by this bullet was already applied in a pre-master-plan commit (`rounded-sm`, which maps to the Chunk-1 `--radius-sm: 8px` token) — re-verified correct against both ground-truth sources agreeing on 8px. Re-auditing the rest of the shared shell (in scope since nothing is pre-verified) found two real color mismatches shared by both ground-truth panels: `modal.tsx` used a fabricated `bg-neutral-4` (#2C2F33) — both real panels use neutral.3 (#393D41) — fixed to `bg-neutral-3`; and a near-invisible fabricated `border-neutral-3/30` — the one real bordered panel (Assignee Modal) specifies a solid 1px neutral.2 border — fixed to `border-neutral-2`. Updated the header divider and close-button hover shade (previously both keyed off `neutral-3`, now indistinguishable against the new `neutral-3` panel bg) to `neutral-4` so they stay visually legible. Removed the fabricated `shadow-2xl` (Tailwind default, zero ground-truth basis — neither real panel spec lists any box-shadow/drop-shadow property, and no established shared-elevation token exists outside Datepicker's panel-specific `--shadow-elevation`, Chunk 16's scope) — the dark `bg-black/60 backdrop-blur-sm` overlay behind the dialog already provides depth separation and is a non-contradicted, necessary addition for the centered-dialog pattern this shell implements (same "kept when non-contradicted" precedent as earlier chunks' spec-free extras). Drive-by: translated leftover Spanish `aria-label`s — `'Cerrar modal'`→`'Close modal'` in `modal.tsx`, `'Puntos de estimación'`→`'Estimate points'` in `estimate-modal.tsx` — per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunks 6-11. `npm run typecheck`, full `vitest run` (38/38, unchanged — `modal.test.tsx`'s 6 tests assert only on behavior/a11y roles, not styling classes), and `npm run build:storybook` all pass.
- [x] Chunk 13: AddTaskModal, AssigneeModal, EstimateModal — cross-check against `Task Column01.md` inline modal spec and `Mockups/Dashboard Add Task/`. Sources: `Components/Task Column01.md` (in-context composite) + all 7 `Mockups/Dashboard Add Task/Add Task Modal00-06.md` isolated-flow exports. Found the whole prior trio was architecturally fabricated: all 3 wrapped the shared centered-dialog `Modal` (backdrop scrim, header/close button, Cancel/Confirm footer) — but none of the 3 real Figma components is a centered dialog. **AddTaskModal** is a 578×184 inline widget composited directly into a Task Column (no backdrop, no header) — rebuilt to drop the `Modal` wrapper entirely, replaced the boxed/labeled `Input` with a borderless title field (Desktop/Body/XL/bold, 20px, neutral-2 placeholder, matching the "borderless dark-surface text field" pattern Chunk 6 flagged and deferred here), and removed a fully fabricated "Description" field with zero ground-truth basis. **AssigneeModal** and **EstimateModal** are small anchored popovers (239×432 / 122×208, neutral-3 bg, 1px neutral-2 border, 8px radius) — rebuilt to drop `isOpen`/`onClose`/backdrop entirely in favor of the same parent-conditionally-mounts convention already established by `DatePickerMenu`, and dropped both fabricated footers (Confirm button, multi-select checkmarks on Assignee; Cancel/"Set N points" on Estimate, plus the entirely fabricated circular Fibonacci-pill `EstimatePoints` sub-component) — every real "User"/"Estimate Points" row instance in the export renders identically with no selected/checked variant, so clicking a row is the confirm action, no separate footer. EstimateModal's 5 rows read as point values 1/2/3/5/8 (row-width evidence: one row's `LABEL` text is narrower than the other 4, matching singular "1 Point" vs. plural "N Points"), replacing the previous fabricated 8-value `[0,1,2,3,5,8,13,21]` set. AddTaskModal's row-2 "Tags" strip has 4 real chip slots in spec; only 2 (order 0/1) ever show a filled state across all 7 export snapshots and read unambiguously as Estimate/Assignee triggers (icon+"N Points" and icon+avatar+name respectively, both losing their muted `Tag`-style background once filled — confirmed via the `rgba(148,151,154,.1)` unfilled bg being an exact pixel match for `Tag`'s existing solid/neutral style); wired both as popover triggers, plus reused the existing due-date `DatePickerMenu` trigger as the 3rd slot (a `Tag`-based chip, consistent with the due-date `Tag` pattern already established on `TaskCard` in Chunk 11). The 4th slot (a ~40px-wide label, never shown filled in any snapshot) has no legible glyph or contradiction-free semantic — left unimplemented, same precedent as Chunk 11's un-glyphed 3rd Reactions slot. Confirmed the Create-button empty/typed-title contrast (`primary-2` disabled → `primary-4` enabled) needs no new code: it's already exactly `TextButton`'s existing disabled/enabled primary styling from Chunk 4, reproduced for free via `isDisabled={!title.trim()}`. All 3 components had zero real consumers outside their own story files, so the `isOpen`/`onClose` signature changes on Assignee/EstimateModal are non-breaking in practice; migrated all 3 `.stories.tsx` files accordingly (dropped the `useModal`-driven Cancel/Confirm demo scaffolding for Assignee/Estimate in favor of the static/`withSurface`-decorated pattern already used by `DatePickerMenu.stories.tsx`). `npm run typecheck`, full `vitest run` (38/38, unchanged — no test file existed for any of the 3 and none was required by this chunk's fidelity-only scope), and `npm run build:storybook` all pass.
- [x] Chunk 14: TaskColumn + TaskListView — header, stacked TaskCards, 20px/24px container radius (post-Chunk-1 token). Source: `Task Column00/03.md`, cross-checked against `Mockups/Dashboard Default View/Dashboard Mockup.md`. Found the whole Chunk-1-era premise was wrong: `Task Column00/02/03.md`'s outer 1470×1818, neutral.5, `border-radius: 20px` frame (the source of `--radius-20`, added in Chunk 1) is not a real "TaskColumn" product component — grepped every file using the same `utils/frameHeader` doc-chrome layer (`Cards00/01.md`, `Datepicker.md`, `SideBarItem00/01.md`, `Avatar00/01.md`, `ApplicationSidebar00.md`, etc.) and found they *all* share this identical 1470×1818/neutral.5 canvas, confirming it's a reused Figma documentation-page master template, not a shipped component's frame — and it isn't even internally consistent (`Datepicker.md`'s copy of the same canvas uses `border-radius: 0px`). Cross-referencing the real in-context `Dashboard Mockup.md` confirmed the actual product layout: `Frame 654` (`flex-direction: row`, `gap: 32px`) lays out 3 "Task List View" instances side by side — each is just a `ProjectInfo` header (already built in Chunk 11) + a stack of `TaskCard`s (`flex-direction: column`, `gap: 16px`), with **no** background, border, radius, count badge, or "add task" affordance on the frame itself in any of the 3 real instances. The previously-shipped `task-column.tsx` (colored accent stripe, uppercase bold title, count-badge pill, dashed-border "Agregar tarea" footer button, `bg-neutral-4 rounded-[20px]` wrapper) had zero ground-truth basis for any of that chrome and zero real consumers outside its own story file — deleted outright (`task-column.tsx`, `task-column.stories.tsx`, its `index.ts` export), same precedent as Chunk 9's fabricated `footer`-prop removal. Removed `--radius-20` from `theme.css`/`colors.mdx` as fabricated (sourced entirely from the doc-page-canvas noise above); confirmed `--radius-xl`'s only remaining flagged consumer is `task-table.tsx` (Chunk 15's job — `project-info.tsx` and `task-column.tsx`'s prior flags are resolved: the former was already rebuilt radius-free in Chunk 11, the latter is deleted). Rebuilt `task-list-view.tsx` to be the real "Task List View": now takes `title`/`icon` (forwarded to a composed `ProjectInfo` header) plus `tasks`, rendering `ProjectInfo` + stacked `TaskCard`s in a `flex-col gap-4` (16px, unchanged from a prior pass) with no added chrome. Added a `Board` story to `task-list-view.stories.tsx` demonstrating the real `Frame 654` composition (3 `TaskListView`s in a `flex-row gap-8` (32px), no wrapping component needed since none exists in spec). Drive-by: translated leftover Spanish story args (`'2 DÍAS'`→`'2 DAYS'`, `'VENCIDO'`→`'OVERDUE'`) and the fabricated component's empty-state/footer copy (`'No hay tareas en esta vista.'`→`'No tasks in this view.'`, `'Agregar tarea'`→ removed with the footer) per the global no-Spanish-in-code-artifacts rule, same class of cleanup as Chunks 6-13. `npm run typecheck`, full `vitest run` (38/38, unchanged — no test file existed for either component and none was required by this chunk's scope), and `npm run build:storybook` all pass (storybook build confirms no orphaned `task-column.stories` chunk).
- [x] Chunk 15: TaskTable + TaskTableRow — resolve the already-flagged boxed-grid vs flat-row structural mismatch against `Task Column02.md`. Source: `Components/Task Column02.md`'s "Task Table Row"/"Table Header Cell"/"Task Cell" definitions, cross-checked against the real in-context "Table View" instance in `Mockups/Task Default View/My Task Mockup.md` (confirmed via its rendered screenshot, `Pasted image 20260803203242.png`, which resolved several CSS-only ambiguities the export text alone couldn't). Found the real structure: a shared column-header row ("# Task Name" 500px / "Task Tags" 168px / "Estimate" 140px / "Task Assign Name" 168px / "Due Date" 132px, summing to the spec's 1108px row width) sits above N status-group boxes ("Task Table"s, one per "To Do"/"In Progress"/"Reviews" section), each starting with a "Task Cell" group header (caret icon + bold title) and stacking its "Task Table Row"s directly beneath with **no** row gap — every cell (in both the header and each row) is individually boxed with its own neutral.4 fill + 1px neutral.3 border, confirming the flagged mismatch: the prior `<table>` used one shared row-bottom-border, not Figma's per-cell boxed grid. Rebuilt entirely: each status group now renders as its own `<table border-collapse>` (native border-collapse merges the individually-bordered `<td>`s into clean hairlines instead of doubling them — the direct fix for the flagged mismatch), stacked in a `gap-4` (16px) column beneath a shared header row, exactly matching the real "Table View" auto-layout (`gap: 16px` between the header and each group-table). Column order/widths corrected to the real spec order (Task Name → Tags → Estimation → Assignee → Due Date; the old table was Task/Assignee/DueDate/Points/Tags, matching nothing in spec). Fixed two real fidelity bugs found along the way: `EstimationCell` was a fabricated numeric badge (rounded pill, `text-xs font-bold`) — spec renders it as plain `Desktop/Body/M/regular` text with no chrome (confirmed via the mockup screenshot showing literal "4 Points"/"2 Points" text, not "N Days" as the isolated doc's placeholder sample text read) — rebuilt as plain pluralized text. `TableHeaderCell` was a generic sortable `<th>` with fabricated uppercase/bold styling and no consumer-facing structural basis — replaced with the real fixed 5-column header (`Desktop/Body/M/regular`, neutral.4 fill, 4px radius only on the two outer corners per spec's literal `4px 0 0 4px`/`0 4px 4px 0` corner values). Added two spec-evidenced elements that didn't exist before: (1) a "Line 1" accent stripe flush against each row's left edge (`indicatorColor` prop reusing the exact 3 hues already verified for `Tag` — primary/secondary/tertiary — since no due-date-urgency or other semantic linkage is evidenced in the export, just an independent per-row color); (2) a checkbox in the Task Name Cell that Figma's own "Property 1=Default" (opacity:0) vs "Property 1=Hover" (opacity:1) states prove is a real hover-reveal, not a fabrication like other previously-rejected unlabeled slots — wired as an optional `isSelected`/`onSelectedChange` pair, staying visible once checked. Left one ambiguous element unimplemented: a 3rd "Reactions"-shaped slot in the Task Name Cell whose one real rendered sample reads "Details >" (a navigation affordance, not a count) — doesn't fit the `count`+`emoji` shape used for the other 2 (wired as a plain `reactions?: TaskTableReaction[]` prop, deliberately not reusing the `Reactions` component from Chunk 11 since its 16px inter-item gap doesn't match this context's real 8px cell gap), same "leave unglyphed/ambiguous slots unimplemented" precedent as Chunk 11's un-glyphed 3rd reaction slot on `TaskCard`. Removed the fabricated `rounded-xl` (40px) wrapper on the old table — this was the last of the 3 consumers `--radius-xl` was flagged against back in Chunk 1 (the other 2, `project-info.tsx`/`task-column.tsx`, were already resolved in Chunks 11/14) — confirmed zero remaining real consumers and removed the token entirely from `theme.css`/`colors.mdx`, same "fabricated token with zero consumers" precedent as `--radius-20`'s removal in Chunk 14. Added `task-table.test.tsx` (3 tests: grouped rendering with per-group-restarting row indices, checkbox toggle behavior, empty-column omission) — previously had zero test coverage, consistent with adding tests when a chunk introduces new interactive/accessibility behavior (Chunks 6/10 precedent). `npm run typecheck`, full `vitest run` (41/41, up from 38/38), and `npm run build:storybook` all pass (the latter also verified the new `rounded-l-4`/`rounded-r-4`/`rounded-t-4` directional radius utilities compile correctly to `var(--radius-4)`).
- [x] Chunk 16: Datepicker + DatepickerMenu. Source: `Components/Datepicker.md`'s "DatePicker / Menu" component, confirmed as the real desktop target via 3 pixel-identical real in-context instances (`Mockups/Task Add Task/Add Task Modal05.md`, `Mockups/Dashboard Add Task/Add Task Modal05.md`, `Mockups/Dashboard Edit Task/Add Task Modal06.md`) — re-confirming the Chunk-1 premise that `Date Picker.md`'s 320×472 DM-Sans "hero" variant doesn't back any shipped desktop component (grepped the full `Mockups` tree: its only hit outside its own doc file is the mobile-Android mockup, out of scope). Found and fixed several real structural gaps in `datepicker-menu.tsx`: (1) header year-navigation was missing entirely — spec's Header has 4 nav icons (DoubleLeft/Left on the left, Right/DoubleRight on the right), the prior implementation only had single-chevron month nav — added `prevYear`/`nextYear` handlers and the two double-chevron icons. (2) both full-width 1px `neutral-2` dividers (header/content, content/footer) were absent — added. (3) the day-grid used a variable 5-or-6-row height depending on the viewed month; spec's Content frame is a fixed 226px (8px padding + 7 rows × 30px, i.e. always 6 week-rows) — changed the cell-generation loop to always pad to 42 cells. (4) the weekday-abbreviation header row (`Row` order 0 in spec, all-white text, no highlighting) was missing — added `Sun–Sat` labels matching the observed per-cell text-width deltas. (5) resolved a mismatch already flagged in the component's own doc comment: the prior code had two competing, only-one-of-which-verified day-cell treatments — a filled `bg-primary-4` "selected" state and a bordered `border-primary-4` "today" state — but the entire Datepicker.md export contains exactly one real highlighted-cell instance anywhere (a 1px solid `primary-4` border, `border-radius: 2px`), no filled-background cell exists at all; collapsed both concepts to the one verified treatment (border only, applied to the selected date), dropping the fabricated fill and the separate unverified "today" ring. (6) day-cell and outer-container radii were using Tailwind's coincidentally-identical default `rounded-xs`/`rounded` instead of the real `--radius-2`/`--radius-4` tokens (both added in Chunk 1 specifically citing this file) — switched to the explicit `rounded-2`/`rounded-4` utilities; swapped the fabricated `shadow-lg` (Tailwind default) for the real 3-layer `--shadow-elevation` token (also Chunk-1-sourced from this exact file). (7) the Footer (a centered, primary-4, SF-Pro-Text-Regular text button with a permanently-hidden search-icon slot) was entirely unimplemented — its label renders as the literal un-customized layer name "Button" in all 3 real instances, so the exact wording is unverified; interpreted as a "Today" quick-jump action (the most conservative, standard fit for a lone accent-colored footer button in a due-date picker, same class of judgment call as Chunk 10's Frame 648 clear-search interpretation) — wired to reset the view to the current month/year and call `onChange` with today's date. Translated the component from Spanish to English per the global no-Spanish-in-code-artifacts rule (`DAYS`/`MONTHS` arrays, nav `aria-label`s, day-cell `aria-label` locale `es-MX`→`en-US`) — same class of cleanup as Chunks 6-15, previously missed here since this file predates the master plan. Confirmed `datepicker.tsx` (native `<input type="date">`) has zero real consumers anywhere in the codebase (grepped all imports) and, per the Chunk-5 `switch.tsx` precedent (a fabricated-but-non-contradicted, genuinely useful standalone primitive is kept rather than deleted, since nothing in spec contradicts a plain native date input existing as a utility), left it unchanged — it was already fixed for `aria-invalid`/`aria-describedby` wiring in Chunk 6 and has no Spanish to translate. Extended `datepicker-menu.test.tsx` with 2 new tests (year navigation, the "Today" footer action) — 6/6 passing, up from 4/4. `npm run typecheck`, full `vitest run` (43/43, up from 41/41), and `npm run build:storybook` all pass (the latter also verified the new `rounded-2`/`rounded-4`/`shadow-elevation` utility classes compile to the correct token values in the build output).

### Validation & Polish
- [x] Chunk 17: Mockup validation pass — spot-check composed layouts against `Mockups/Dashboard *` and `Mockups/Task *` for integration issues invisible at the component level. Cross-checked the real in-context page shells in `Mockups/Dashboard Default View/Dashboard Mockup.md` and `Mockups/Task Default View/My Task Mockup.md` (both 1440×900 canvases) — Chunks 8-10 only ever verified `ApplicationSidebar`/`TopNav` in isolation and no story anywhere composed a full page, so nothing had cross-checked their combined positions until now. Found the two pages share one pixel-identical shell that no component or story reproduced: `Sidebar` (`left:32 top:32`) + `Search Bar`/`TopNav` (`left:296 top:32 right:36 h:64` — flush against the sidebar with the same 32px gap as the page's own margin) + a previously undiscovered "Top Bar" row at `top:128 h:48` (`justify-content:space-between`) containing a `Swicter` view-switcher on the left and a lone primary-filled icon `Button` on the right + the main content 16-24px below that (`Frame 654`'s Task List View row on the Dashboard at `top:192`, the "Table View" list on My Tasks at `top:200` — an 8px delta treated as canvas-measurement noise, not a real distinct offset, same class of judgment call as Chunk 15's cell-width hug-noise). The "Swicter" (`Button, Switch Button01.md`'s own isolated "Swicter" doc block, previously dismissed in Chunk 7 as "an unrelated icon-button pair") turned out to be a real, twice-repeated page fixture: an 80×40 `neutral.5`/8px-radius wrapper around exactly two of the existing icon `Button`s (`variant="secondary"`), toggling which one carries `isSelected` — confirmed via `Property 1=Right Selected`/`Left Selected` in the doc export. Both real page instances render the identical selected side regardless of card-board vs. table content, so nothing in spec ties a given side to a content type — built `ViewSwitcher` (`src/components/layout/view-switcher.tsx`) with purely positional `value: 'left' | 'right'` (no fabricated board/list semantic) and consumer-supplied icon slots (source vector paths aren't legible enough to reproduce faithfully, same "leave the unglyphed thing unimplemented" discipline as Chunks 11/13/15's unglyphed slots). Promoted the shared page scaffold itself into `AppShell` (`src/components/layout/app-shell.tsx`) — not a named Figma component, but a real, consistent composition repeated identically on two independent mockups, which the "reusable layout shell" carve-out in this chunk's brief explicitly anticipated; kept its API to exactly what's evidenced (`logo`, `sidebarItems`, `topNavProps`, an optional `topBar` slot, `children`), using one uniform 32px page padding rather than fabricating the source canvas's fixed-1440px 36px-right-edge asymmetry. Added `Layout/AppShell` stories (`Dashboard`, `TaskDefaultView`) demonstrating both real page compositions end-to-end with `TaskListView`/`TaskTable` respectively, and a standalone `Layout/ViewSwitcher` story. Checked popover anchoring (`AssigneeModal`/`EstimateModal`/`DatePickerMenu`, all `top-full left-0 mt-1` off their trigger chip) against `Add Task Modal05.md`'s in-context `DatePicker/Menu` instance — that export's popover has no `position`/`left`/`top` values tying it to the trigger (only a decorative "Hand-OneFinger-Tap" cursor illustration), so the existing anchoring is already the most defensible reading and needed no change. Checked `Dashboard Edit Task/Add  Task Modal00-07.md` (source folder name has a double space) and found it reuses the literal `/* Add Task Modal */` component reopened with the Estimate ("0 Points") and Assignee ("Jerome Bell") triggers already filled — confirming Edit is the same widget pre-populated, not a distinct component or flow; `AddTaskModal` previously had no way to seed non-empty state, so added optional `initialTitle`/`initialDueDate`/`initialPoints`/`initialAssignee` props (all default to prior create-flow blank behavior, non-breaking) and an `Edit` story. Checked `Dashboard Delete Task/Dashboard Mockup00/01.md` for a delete-confirmation affordance — grepped both exports and the full `Mockups` tree for "delete"/"confirm"/"trash"/"kebab"/"dialog"/"popup", zero hits anywhere; the only observable delta between the two snapshots is fewer task cards in `01.md`, consistent with an instant removal with no confirmation UI in spec — left unbuilt rather than fabricating a confirmation modal with no evidentiary basis. `Task Add Task`/`Task Default View`'s Add Task Modal flow files mirror `Dashboard Add Task`'s 1:1 (same isolated component, same step progression) and are covered by the same Chunk 13 audit plus this chunk's edit-mode addition — no separate findings. `npm run typecheck`, full `vitest run` (43/43, unchanged — this chunk's findings were composition/integration fixes, not new component behavior requiring new unit tests), and `npm run build:storybook` all pass (confirms `app-shell.stories`/`view-switcher.stories` compile and bundle correctly).
- [x] Chunk 18: Premium polish pass — hover/focus/transition micro-interactions benchmarked against design-system.schoolai.com; empty/loading states. This chunk has no Figma ground truth to audit against (static exports have no interaction states) — instead, live-inspected `design-system.schoolai.com`'s Storybook via browser automation (computed styles on its Button/Checkbox stories) to establish a real benchmark rather than guessing: hover uses Tailwind's unmodified `transition-colors` (150ms `cubic-bezier(0.4,0,0.2,1)`, already this kit's prevailing pattern — no token/duration changes needed), and every interactive element gets a `focus-visible:outline-2 focus-visible:outline-{accent}` treatment (their exact pattern additionally uses `ring-2`/`ring-offset-2`, but ring-offset needs a per-surface offset *color* to look right and this kit has many different dark surface tones — `outline`+`outline-offset` achieves the same visible-gap effect without that per-surface coupling, so kept the kit's pre-existing outline convention from `input.tsx`/`switch.tsx` and extended it everywhere instead of introducing a second pattern). Audited all interactive components and found the accessible-focus story was the real gap: 14 files (`button.tsx`, `text-button.tsx`, `sidebar-item.tsx`, `segmented-control.tsx`, `reactions.tsx`, `user-row.tsx`'s button variant, `top-nav.tsx`'s clear-search button, `modal.tsx`'s close button, all 3 `add-task-modal.tsx` triggers, `assignee-modal.tsx`/`estimate-modal.tsx` rows, `datepicker-menu.tsx`'s 4 nav buttons + day cells + footer button) had zero focus handling at all — keyboard users tabbing through got no visible indicator anywhere. One had an active bug: `tag.tsx`'s remove button used `focus:outline-none` with nothing replacing it, suppressing the browser's default focus ring for every input modality (not just mouse) — fixed to the standard `outline-none` + `focus-visible:outline-*` pair. Two components had a worse variant of the same bug: `task-table.tsx`'s row-select checkbox and `label-checkbox.tsx` both hide their real `<input>` via `sr-only` (clipped, not `display:none`, so it's still focusable) but had nothing to show a keyboard focus location — fixed via `has-[:focus-visible]:outline-*` on the wrapping `<label>` (also added `group-focus-within:opacity-100` to `task-table.tsx`'s checkbox glyph, which was previously only ever revealed via `group-hover`, so tabbing to it with a mouse elsewhere on the row showed nothing). `task-card.tsx` had a separate, more fundamental issue: the whole card is a `<div onClick>` with no keyboard path to it at all — added `role="button"`/`tabIndex={0}`/an Enter-and-Space `onKeyDown` handler plus the focus-visible outline, gated on `onClick` being provided so non-interactive card usages are untouched. Standardized 4 components already carrying *some* focus treatment onto one consistent convention: `tabs.tsx` used a different `ring-2`/`ring-primary-4` pattern (no offset) — converted to the kit-wide outline pattern (with a negative offset, since tabs sit flush in a row with no room to bleed outward, same reasoning applied to `sidebar-item.tsx`/`assignee-modal.tsx`/`estimate-modal.tsx` rows, which are also flush, full-bleed rows); `switch.tsx`/`input.tsx`/`datepicker.tsx` already had the right pattern but no `outline-offset`, added `outline-offset-2` for the visible-gap consistency the rest of this pass establishes. Datepicker's 24×24 day cells intentionally got no offset (packed tight with only 3px row spacing — an offset ring would visually clip into neighboring cells). Empty/loading states: `task-list-view.tsx` already had a real empty state from a prior pass (confirmed, left as-is); `task-table.tsx` had none — added a matching centered message when `groups` is empty. Loading has no Figma basis at all (same class of gap as the hover/focus states above) — added a standalone `Skeleton` primitive (`src/components/skeleton/skeleton.tsx`, `animate-pulse` block, same "genuinely useful non-contradicted utility" precedent as `switch.tsx`/`datepicker.tsx` in Chunks 5/16) and wired `isLoading` props into `TaskListView` (3 skeleton task-cards) and `TaskTable` (5 skeleton rows under the real header), with `Loading`/`Empty` stories added to both plus a new `Skeleton` stories file. Drive-by: found and translated leftover Spanish story args in `badge.stories.tsx` (`'Borrador'`→`'Draft'`, `'Completado'`→`'Completed'`, `'En Progreso'`→`'In Progress'`, `'Bloqueado'`→`'Blocked'`) per the global no-Spanish-in-code-artifacts rule — Badge was never in scope for any prior chunk (it isn't tracked anywhere in this plan's component list) so this was the first pass to touch the file. `npm run typecheck`, full `vitest run` (43/43, unchanged — this chunk's changes are interaction/a11y polish, not new component logic requiring new unit tests), and `npm run build:storybook` all pass (confirms `skeleton.stories` compiles and bundles, and the `has-[:focus-visible]:` / `group-focus-within:` Tailwind v4 arbitrary variants used above resolve correctly).
- [x] Chunk 19: Documentation pass — finalize `colors.mdx`/`typography.mdx` (remove "pending Figma verification" banners), add intro/overview docs, ensure `storySort` hierarchy is complete. Found `colors.mdx`/`typography.mdx` already had their "pending Figma verification" banners removed back in Chunk 1 (re-verified via grep across `src`/`README.md` — zero hits for that phrase anywhere) — no change needed there. Added `src/styles/introduction.mdx` (`<Meta title="Introduction" />`, no parent category so it renders as its own top-level page) — an overview page explaining what the kit is, how the 4 documentation categories (Design Tokens/Primitives/Components/Layout) are organized and what belongs in each, install/usage instructions, and the fidelity caveats already established across prior chunks (no semantic color-alias layer yet, literal-not-ramped radii, the handful of spec-free-but-kept primitives from Chunks 5/16/18). Updated `.storybook/preview.ts`'s `storySort.order` to put `'Introduction'` first (previously `['Design Tokens', [...], 'Primitives', 'Components', 'Layout']` already covered every real top-level category with zero gaps — confirmed via a full grep of every `title:` string across all `.stories.tsx` files — so the only real gap was the newly-added Introduction page itself). Fixed a real drift bug found while writing the install example for the new intro page: **`README.md`'s own "Usage in your project" example was broken** — it called the generic-looking `<Button variant="primary" size="md" onPress={...}>Guardar Usuario</Button>`, but Chunk 4 rebuilt `Button` into a 40×40 icon-only component with a required `aria-label` and no `size` prop and no text-children support months ago; the labeled-CTA use case the example was demonstrating is actually `TextButton` — fixed the example to use `TextButton` (which does accept text children and has no `size` prop) instead of `Button`, so a developer copy-pasting the README no longer hits a type error. Also found `README.md`'s design-token list was stale (still listed the removed `--radius-xl`, missing `--radius-2`/`--radius-4`/`--radius-10`/`--color-blue`/the 3 shadow tokens, all added across Chunks 1-16) — brought current against `theme.css`. Translated the entire `README.md` from Spanish to English (headers, prose, and the broken code example's Spanish strings) per the global no-Spanish-in-code-artifacts rule — it predates the master plan and had never been touched by any prior chunk's drive-by sweep. A fresh full-repo grep for Spanish (accented characters, since prior chunks' targeted greps for specific known strings could miss stragglers) turned up 2 more previously-missed files outside any prior chunk's scope: `card.stories.tsx` ('Título de la Tarjeta'/'Contenido explicativo...' → 'Card title'/'Explanatory card content within the interface.') and `switch.stories.tsx` ('Notificaciones'/'Modo oscuro'/'Opción deshabilitada' → 'Notifications'/'Dark mode'/'Disabled option') — both fixed, same class of cleanup as Chunks 6-18. `npm run typecheck`, full `vitest run` (43/43, unchanged — this chunk is documentation-only, no component behavior changed), and `npm run build:storybook` all pass (confirms `introduction.mdx` compiles to its own `introduction-*.js` chunk and the new `storySort` order doesn't break the build).

### Tooling & Hygiene (post-audit)
- [x] Chunk 20: Tooling & Hygiene cleanup. After all 19 chunks above were checked off and verified green, a two-agent read-only audit answering "is this 100% finished" found 4 concrete gaps unrelated to Figma fidelity (as opposed to a separate set of genuine fidelity judgment calls — `switch.tsx`'s zero ground-truth basis, `top-nav.tsx`'s pixel-width-guessed clear-search affordance, `datepicker-menu.tsx`'s guessed "Today" footer label — which need live Figma access and are explicitly NOT touched here). Fixed all 4: (1) **ESLint didn't exist at all** (no config, no script) — added `eslint.config.js` (flat config) composing `@eslint/js` + `typescript-eslint` recommended + `eslint-plugin-react-hooks` (`recommended-latest`, applied via a manually-declared `plugins` object rather than spreading the preset directly — the installed `eslint-plugin-react-hooks@7.1.1`'s `configs['recommended-latest'].plugins` resolves to an array at runtime, not the object flat config requires, discovered by bisecting the config file after ESLint's own error output blamed the merged config rather than a specific plugin) + `eslint-plugin-jsx-a11y` (`flatConfigs.recommended`) + `eslint-plugin-storybook` (`flat/recommended`, scoped to `*.stories.tsx`/`.storybook/main.ts` internally). Pinned `eslint`/`@eslint/js` to `^9` rather than the newly-released `10` — `eslint-plugin-jsx-a11y@6.10.2`'s peer range only goes up to `^9`, confirmed via `npm view` before pinning rather than discovering it via a failed install. Running it surfaced exactly 2 real violations, both `jsx-a11y/no-autofocus`: `modal.tsx`'s react-aria `<FocusScope autoFocus>` and `add-task-modal.tsx`'s inline title-field `autoFocus` — both are the WAI-ARIA-recommended dialog/widget open-focus pattern (not the raw-autofocus-on-page-load anti-pattern the rule targets), so fixed with a targeted, justified `eslint-disable-next-line` on each rather than removing correct behavior or disabling the rule kit-wide (learned mid-fix that the directive comment must be the single line immediately above the flagged attribute, not a multi-line block several lines above it — an `eslint-disable-next-line` a few lines too high produces both an "unused directive" warning at the comment and a live error at the real line). Added `"lint": "eslint ."` to `package.json` and `npm run lint` as the first step of `CONTRIBUTING.md`'s "Before committing" checklist. (2) Fixed `CONTRIBUTING.md:37`'s stale `Layout/*` example (still named the long-deleted `TaskColumn`) — cross-checked real current `title:` strings across `src/components/**/*.stories.tsx` before rewriting it, rather than reconstructing the list from memory. (3)-(5) Added the 3 test files the audit flagged as missing per `CONTRIBUTING.md`'s own testing rule: `text-button.test.tsx` (4 tests, mirroring `button.test.tsx`'s existing pattern plus one the original didn't have — `onPress` not firing when `isDisabled` and clicked, the fuller letter of the CONTRIBUTING rule), `search-bar.test.tsx` (5 tests: accessible name/placeholder, uncontrolled typing + onChange, Enter→onSubmit, and controlled mode not self-mutating its displayed value), and `task-card.test.tsx` (5 tests covering the manually-added Chunk 18 keyboard-a11y logic: `role`/`tabIndex` present only when `onClick` is provided, click/Enter/Space all trigger it). All new tests passed on first real run against the already-shipped implementations — no component code changes were needed to make them pass, only the tests themselves were missing. `npm run lint`, `npm run typecheck`, `npm run test` (57/57, up from 43/43), `npm run build`, and `npm run build:storybook` all pass.
- [x] Chunk 21: Figma-verified fidelity fixes (3 of 3 — all resolved; the 3rd was completed via the user's own manual Figma check after the MCP quota blocked doing it via tool calls, see addendum below). Got live Figma access (fileKey `ZUAB3jXFyKFktoAzvN7h1T`, from `1. UI-Kit.md`) and pulled `get_design_context` on the 2 highest-value ambiguous nodes before hitting a hard quota: the account is a View seat on a Starter plan, capped at 6 Figma MCP tool calls/month — confirmed via the `whoami` tool and the rate-limits doc, not assumed. **Switch (115:3679, "Switch button")**: confirmed fabricated with certainty, not just absence of evidence — the real component isn't a boolean toggle at all, it's a single 157px pill/chip ("Dashboard" label, Default=transparent/neutral-2 text vs Selected=neutral-2 fill/white text/Drop Shadow Small), dimensionally and stylistically identical to what `segmented-control.tsx` already correctly implements (independently re-confirms Chunk 5's `13px/13px/1px-tracking` typography fix was exactly right). Since a real, contradicting "Switch button" component now exists in evidence, `switch.tsx` no longer meets this project's own bar for a kept-but-unverified primitive ("kept because genuinely useful and doesn't contradict anything in spec" — Chunk 5/`introduction.mdx`) — deleted `switch.tsx`/`switch.stories.tsx`/`switch.test.tsx` outright (zero real consumers, confirmed via grep before deleting), removed its `index.ts` export, and updated `CONTRIBUTING.md`/`introduction.mdx`'s Primitives lists and fidelity-notes section accordingly. **TopNav search bar (82:2742, "Search Bar")**: confirmed correct — Selected state is the focused-input state (a bare text-cursor glyph replaces the "Search" placeholder) and does show an extra close/X icon, exactly the clear-search interpretation Chunk 10 guessed; colors/radius/gaps/fonts all matched the shipped implementation exactly (bg `#2c2f33`=neutral-4, `16px` radius=`--radius-md`, `24px` gaps=`gap-6`). One real bug found in the process: the close icon is the FIRST child of the trailing icon group in the real component, not appended after the bell icon as shipped — fixed the DOM order in `top-nav.tsx` (verified via `top-nav.test.tsx`, still 4/4 passing — the tests use role/label queries, not order-dependent). Left one open, honestly-labeled ambiguity in the updated doc comment: whether the real trigger is "focused" vs the shipped "has a non-empty value" (Selected's cursor glyph doesn't disambiguate this) — kept the value-based trigger as the better UX default, not a confirmed fact. **`datepicker-menu.tsx`'s footer "Today" label** — blocked by the exhausted monthly Figma quota via MCP tool calls; resolved instead by the user directly inspecting node `679:18197` ("DatePicker / Menu") in their own logged-in Figma session (a workaround that doesn't touch the rate-limited API at all) and sharing a screenshot of the Dev Mode panel. The screenshot's "Content" field showed the button's actual rendered text is literally "Today" — the earlier static CSS export had only captured the layer's *name* ("Button"), which is a different thing from its rendered content, and couldn't distinguish the two. This confirms the previous guess was exactly right; updated the doc comment in `datepicker-menu.tsx` to state the label is now a confirmed fact rather than an interpretation, no behavior change needed. `npm run lint`, `npm run typecheck`, `npm run test` (54/54, down from 57/57 — `switch.test.tsx`'s 3 tests were removed along with the component), `npm run build`, and `npm run build:storybook` all pass.

### Design Token Follow-ups (post-audit)
- [x] Chunk 22: Semantic color-alias layer. Added 6 additive aliases to `theme.css`'s `@theme` block — `--color-main`/`--color-muted`/`--color-interactive`/`--color-danger`/`--color-surface-neutral`/`--color-subtle` — every one aliasing an already-verified ramp token (e.g. `--color-main: var(--color-neutral-1)`), no new hex introduced. Named as bare nouns rather than property-prefixed names (`--color-main`, not `--color-text-main`) because Tailwind v4 generates `{property}-{token-name}` utilities — the property-prefixed form would have produced the doubled, unusable class `text-text-main` instead of the `text-main` `introduction.mdx`/`colors.mdx` had already committed to verbatim; confirmed via a real compiled-CSS check (`.text-main{color:var(--color-main)}`, no `.text-text-main` present) before touching any consumer. `--color-muted` and `--color-subtle` both intentionally alias `neutral-2` — same ramp step, different role depending on which property consumes it. Migrated exactly 4 files as a deliberate proof-of-concept subset rather than a full sweep: `task-card.tsx` (`text-neutral-1`→`text-main` ×2, `hover:border-neutral-2`→`hover:border-subtle`), `input.tsx` (`bg-neutral-1`→`bg-surface-neutral` ×2, `border-neutral-2`→`border-subtle`, `placeholder:text-neutral-2`→`placeholder:text-muted`, `text-danger-5`→`text-danger`), `card.tsx` (`bg-neutral-1`→`bg-surface-neutral`, `border-neutral-2`→`border-subtle`), `sidebar-item.tsx` (active-state `text-primary-4`→`text-interactive`). Reasoning for the partial migration (unlike Chunk 23's full sweep): choosing which alias fits a given raw-ramp call site is a real per-site judgment call (e.g. `neutral-2` means "muted text" in one place and "border" in another), not a safe mechanical find-and-replace — the remaining ~40 call sites across the kit intentionally still use raw ramp tokens directly, documented as a tracked future cleanup rather than silently left unmentioned. Dark-surface-hierarchy aliases (`neutral-3`/`neutral-4`/`neutral-5` as card-bg vs. hover-bg vs. shell-bg) were considered and explicitly deferred — real roles, but needing more design judgment than this initial small set. Updated `colors.mdx` (added the alias table, removed the stale "no semantic aliases yet" banner), `introduction.mdx` (corrected the stale Fidelity-notes bullet), and `README.md` (corrected both stale sentences in the Design Tokens Architecture section, added the new tokens to the enumerated list).
- [x] Chunk 23: Type-scale token layer. Surveyed every real (non-story/test) `text-[...]`/`leading-[...]`/`tracking-[...]` occurrence across `src/components/**/*.tsx` and clustered them into exactly 7 distinct, real, already-in-use tuples — no invented scale. Added each as a `--text-{name}` token in `theme.css` with Tailwind v4's paired `--text-{name}--line-height`/`--text-{name}--letter-spacing` properties (font-*weight* deliberately kept separate, applied via the existing `font-normal`/`font-semibold` utilities alongside the size token, since e.g. `body-m` is used at both weights in real code): `--text-body-m` (15px/24px/0.05em, 12 occurrences, matches the already-documented `Desktop/Body/M` spec from Chunks 2/3/8/11), `--text-body-l` (18px/32px/0.75px, 2 occurrences, `Desktop/Body/L/bold` from Chunk 11), `--text-body-xl` (20px/32px/0.05em, 3 occurrences, `Desktop/Body/XL/bold` from Chunks 6/13), `--text-body-sm` (14px/22px, no letter-spacing override, 4 occurrences, code-consolidated only — no Figma name, per an explicit decision not to spend this kit's scarce Figma MCP quota sourcing the Branding-spec `Desktop/Display/*`/`Desktop/Body/*` ladder this round), `--text-field-label` (12px/16px/0.05em, 2 occurrences, code-consolidated only), `--text-tab-label` (13px/20px/0.25px, 1 occurrence, `Android/Body/S/regular` from Chunk 7), `--text-control-label` (13px/13px/1px, 1 occurrence, `iOS/Subheadline/S/regular` from Chunk 5) — the latter two kept as their own bare single-consumer tokens rather than forced into the `body-*` ladder, mirroring the existing `--radius-2/4/10` precedent (a real value kept as its own token rather than inventing a blended t-shirt size). Explicitly excluded from tokenization (documented, not silently dropped): plain `text-xs`/`text-sm` usages that already exactly match Tailwind's own defaults; `user-row.tsx`'s `roleTextSizes` (its own comment admits "scaled by eye," no spec — tokenizing an admitted guess would launder it as verified); `modal.tsx`'s single `text-base font-bold`; icon-sizing `text-base leading-none` on glyphs. Unlike Chunk 22's partial color-alias migration, migrated **all 24 real call sites in the same pass** — every cluster is a byte-for-byte value match, not a judgment call, so leaving any legacy `text-[15px] leading-6 tracking-wider`-style class sitting unmigrated next to the new token would create two competing sources of truth for the same visual spec, worse than not building the layer at all. Files touched: `text-button.tsx`, `reactions.tsx`, `task-card.tsx`, `task-table.tsx` (incl. the shared `CELL_TEXT` constant), `add-task-modal.tsx` (×2), `estimate-modal.tsx` (×2), `assignee-modal.tsx`, `sidebar-item.tsx`, `label-checkbox.tsx`, `tag.tsx`, `search-bar.tsx`, `user-row.tsx`, `project-info.tsx`, `datepicker-menu.tsx` (×4), `input.tsx`, `datepicker.tsx`, `tabs.tsx`, `segmented-control.tsx`. Updated `typography.mdx` (added the full type-scale table, removed the stale "no type-scale yet" paragraph, explicitly noted the full `Desktop/Display/*` ladder remains a distinct, still-deferred follow-up) and `README.md`. `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass with zero behavioral change (values are unchanged, only class names changed).
- [x] Chunk 24: Targeted Figma verification of `reactions.tsx`'s active-state styling and `task-card.tsx`'s chevron/3rd-reaction-slot ambiguity, spending only 1 of a hard-capped 2 Figma MCP tool calls (fileKey `ZUAB3jXFyKFktoAzvN7h1T`, no specific nodeId — a single comprehensive file-level `get_figma_data` call surfaced everything needed; the 2nd budgeted call was never used). **Active-reaction styling**: confirmed via a full-file structural check that "Reactions" (#53:17490) has no variant set at all — a single static COMPONENT, not a COMPONENT_SET — and every one of its 505 instances across the entire file (every Task Card occurrence, every mockup) uses the identical no-fill/no-border template, with no instance ever deviating. This is materially stronger evidence than Chunk 11's original "no active-state instance was captured" (sample-based absence) — it's now an exhaustive, whole-file-confirmed absence. Per this project's own established bar ("kept because genuinely useful and doesn't contradict spec" — Chunk 5/`introduction.mdx`, the same bar that spared Skeleton/Datepicker's native input), the active border+tint styling is **not contradicted** (unlike Chunk 21's Switch, where a real, *contradicting* component was found) and remains functionally necessary for a working toggle — kept unchanged, comment strengthened rather than the styling. **Chevron/3rd-reaction-slot ambiguity**: resolved with more precision than previously possible. `task-card.tsx`'s "trailing chevron" is confirmed to be the exact same Icon Placeholder slot `ProjectInfo` already exposes via its own `icon` prop (`TaskCard` just never passes one through) — but the glyph itself is a generic, unnamed placeholder in the export, confirmed distinct from the file's real "Arrow Chevron Back/Forward" icons (different component IDs found elsewhere in the same file), so the earlier "chevron/expand" description was corrected as an unconfirmed guess, not left standing. **Significant unplanned discovery**: the 3 real icon instances inside Task Card's Reactions row resolve to named Figma components — `remix-icons/line/editor/attachment-2`, `remix-icons/line/editor/node-tree`, `remix-icons/line/communication/chat-3-line` — not emoji glyphs at all. This suggests the real Figma row may represent attachment/subtask/comment count badges, not user-togglable emoji reactions, a potential mismatch with `Reactions`'s current `emoji`/`isActive`/`onToggle` API. Deliberately NOT acted on in this pass — redesigning a public component API is a product decision beyond what a styling-verification call authorizes on its own — flagged prominently in both `reactions.tsx`'s and `task-card.tsx`'s doc comments as a follow-up decision for the user. Updated both files' doc comments only; no styling/behavior changed. Task-table's 3rd "Details" slot, add-task-modal's 2 tag-strip slots, and `SidebarItemWithOptions` were deliberately left untouched this round (highest-value-only spend, not full coverage, per user's explicit choice). `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass — comment-only changes, zero behavioral impact.
- [x] Chunk 25: Resolved the 3 remaining ambiguous slots deferred from Chunk 24, entirely from data already captured in that same single Figma call (zero additional MCP tool calls spent — 1 of 2 budgeted calls used across both chunks combined). **`task-table.tsx`'s 3rd slot**: confirmed wrong — the prior "no legible glyph/count content" claim was false. The real 3rd "Reactions"-shaped instance in "Task Table Row" has fully legible content: literal text "Details" + icon `remix-icons/line/system/arrow-right-s-line`, a "View Details" link, not another count widget. Implemented as a new `onViewDetails?: () => void` prop on `TaskTableRowProps`, rendering a "Details" + right-chevron link after the existing reactions, matching the confirmed text/icon exactly (no invented styling — reuses `CELL_TEXT`). **`add-task-modal.tsx`'s tag-strip**: also confirmed wrong in a different way — not "2 ambiguous empty slots," but exactly 4 real triggers total (Estimate/Assignee/Label/Due date), meaning exactly 1 trigger ("Label", icon `remix-icons/fill/finance/price-tag-3-fill`) was missing, and it's fully legible, not ambiguous. Added a new `label-modal.tsx` component (`LabelModal`/`Label` type, modeled on `AssigneeModal`'s shell) and wired a Label trigger into `add-task-modal.tsx` between Assignee and Due date, exported from `src/index.ts`. Explicitly documented the boundary of what's Figma-confirmed vs. not: the trigger's icon/text/position are confirmed; `LabelModal`'s popover shell, list layout, and selection behavior have no captured anatomy anywhere in the export (unlike Estimate/Assignee/Due date, whose popovers were separately captured) and are an engineering-only addition, same bar as `Skeleton`/`Datepicker`'s native input — this was an explicit user decision (offered "trigger only, no popover" as the lower-risk default; user chose the full picker instead). Filled-state rendering deliberately keeps the real `Tag` chip (not Estimate/Assignee's "drop to plain icon+text" convention) since Label's entire value *is* its color-coded chip. **`SidebarItemWithOptions`**: re-confirmed via the same full-file data — the "Sidebar Tab" COMPONENT_SET has exactly 3 variants (Default/Hover/Selected), no 4th, no anatomy anywhere beyond the one caption sentence already known. No change; existing documentation already accurate, now backed by an exhaustive check rather than a partial one. Added `label-modal.stories.tsx`, updated `add-task-modal.stories.tsx` (labels + initialLabel args) and `task-table.stories.tsx` (onViewDetails example). `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass.
- [x] Chunk 26: Resolved the Reactions/TaskMetaBadges API mismatch flagged in Chunk 24, per an explicit user decision to do the full redesign (offered "leave as documented," "rename only," and "full redesign" — user chose the breaking redesign, not the safer options). Renamed `reactions.tsx`/`Reactions`/`Reaction` to `task-meta-badges.tsx`/`TaskMetaBadges`/`TaskMetaBadge` throughout (component, story, barrel export in `src/index.ts`, `introduction.mdx`'s Components list) — Figma's own component is still literally named "Reactions" in the design file, so doc comments describing *that* keep the old name; only this library's public API changed. Dropped `isActive`/`onToggle` entirely (attachments/subtasks/comments aren't things a user toggles — confirmed in Chunk 24 there's no active/pressed state in spec at all for this component). Changed `emoji: string` to `icon: React.ReactNode` (matching the icon-prop convention already used by `Tag`/`AddTaskModal`'s triggers) since the real content is icon components (`attachment-2`/`node-tree`/`chat-3-line`), not emoji characters. Made `count` optional so the confirmed icon-only leading badge (Chunk 11/24's "un-glyphed 3rd reaction slot") is just a badge with `count` omitted, rather than a special case — this incidentally resolves that old ambiguity as a side effect of the type redesign, not a separate fix. Added a required `label` prop (e.g. `"3 comments"`) used as the badge's accessible name (`aria-label` on the wrapping `<span>`, with the visual count+icon marked `aria-hidden` to avoid double-announcing) — read-only badges are now `<span>`s, not `<button>`s, since there's no interaction left to expose. `TaskCard`'s `reactions`/`onReactionToggle` props became a single `metaBadges` prop; migrated `task-card.stories.tsx` accordingly (emoji strings → local icon components, matching the pattern already used for `add-task-modal.tsx`'s trigger icons). `task-table.tsx`'s separate, already-read-only `TaskTableReaction` type was deliberately left untouched — it never had `isActive`/`onToggle` to begin with, so it wasn't part of the mismatch, and its cell-based rendering doesn't fit `TaskMetaBadges`'s own layout wrapper. `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass.
- [x] Chunk 27: Fixed the pre-existing moderate `uuid` vulnerability without touching Storybook. Dry-run tested `npm audit fix --force --dry-run --json` first (zero files touched, verified via `git diff`) and confirmed it would genuinely break the repo, not just be "a breaking change" in the abstract: it downgrades `@storybook/addon-essentials`/`@storybook/csf-plugin`/`@storybook/addon-highlight` from `8.6.14` to `7.0.6` while `storybook` core and every sibling addon stay at `8.6.x`, replaces ~10 addon packages with nested 7.0.6-scoped copies, downgrades `@mdx-js/react`, rewrites ~2,271 lines of the lockfile, and force-overrides an `ERESOLVE` conflict (`@storybook/addon-actions@7.0.6` peer-requires React 16-18 against this repo's React 19). Independently confirmed via `package-lock.json` that `uuid` appears exactly once in the entire dependency tree (only under `@storybook/addon-actions`, which calls only the stable `uuid.v4()` export) — added an `overrides` field to `package.json` pinning `uuid` to `^11.1.1` instead, ran `npm install` (not `audit fix`), and confirmed `uuid` now resolves to `11.1.1` under the *unchanged* `@storybook/addon-essentials@8.6.14`. Lockfile diff: 4 insertions/5 deletions. `npm audit` now reports 0 vulnerabilities. `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass with zero disruption.
- [x] Chunk 28: Migrated the ~40 remaining raw-ramp color call sites Chunk 22 deliberately left un-migrated. Full survey (22 files, 75 real occurrences) found 69 "obvious fit" cases directly paralleling how the 4 originally-migrated files (`task-card.tsx`/`input.tsx`/`card.tsx`/`sidebar-item.tsx`) already use each alias, plus a couple of reasonable footnote extensions (`text-button.tsx`'s disabled-state text → `text-muted`; `badge.tsx`'s danger variant → `text-danger`) — migrated all of these across 25 files total (the 21 the survey covered, plus `task-card.tsx` and `sidebar-item.tsx` themselves, which the survey had excluded as "already migrated" but which still had leftover un-migrated occurrences of *other* alias-eligible classes the Chunk 22 pass hadn't touched — a real gap in scoping caught by a final full-codebase grep sweep, not by the survey). Explicitly excluded from aliasing, documented in-code with a comment at each site rather than silently converted: `avatar.tsx`'s `text-primary-4` on initials (a decorative accent-tint/accent-text pairing, not an interactive affordance), `task-table.tsx`'s `text-primary-4` on the "overdue" due-date status text (a status signal, not interactivity), and `tag.tsx`'s `text-primary-4` on `Tag`'s own `variant="primary"` categorical color system (aliasing it would wrongly imply every primary tag is interactive). Final grep sweep across every pattern (`text-neutral-1/2`, `bg-neutral-1`, `border-neutral-2`, `text-danger-5`) confirmed zero real remaining occurrences outside comments. `npm run lint`, `npm run typecheck`, and `npm run build:storybook` all pass.
- [x] Chunk 29: Added the "dark-surface-hierarchy" aliases Chunk 22 explicitly deferred as needing more design judgment. Full survey found `neutral-3`/`4`/`5` each serve multiple genuinely different roles (not a clean 1:1 alias-per-ramp-step the way neutral-1/2 mostly were), so rather than force every usage into one name, added exactly 3 new tokens for the roles actually consistent across 3+ real consumers: `--color-surface-overlay` (`neutral-3` — floating popover/dialog/modal backgrounds: `modal.tsx`, `estimate-modal.tsx`, `add-task-modal.tsx`, `assignee-modal.tsx`, `label-modal.tsx`), `--color-surface-panel` (`neutral-4` — card/container/panel backgrounds: `segmented-control.tsx`, `application-sidebar.tsx`, `task-card.tsx`, `task-table.tsx` cells/group-header, `task-list-view.tsx`, `top-nav.tsx`), and `--color-surface-shell` (`neutral-5` — outermost/deepest backgrounds: `app-shell.tsx`, `view-switcher.tsx`, `datepicker-menu.tsx`'s dropdown panel). Named as part of the same `surface-*` family `--color-surface-neutral` (Chunk 22) already established, at different depth tiers, specifically to avoid the naming-collision risk a bare `--color-surface` would have sitting next to `surface-neutral`. Deliberately left un-aliased, documented in `theme.css`/`colors.mdx` rather than silently dropped: `neutral-3` as a hover/selected-state background (`text-button.tsx`, `datepicker-menu.tsx`'s day-hover, `sidebar-item.tsx`'s badge pill) and as a border/label-text (`task-table.tsx` cells, `input.tsx`/`datepicker.tsx` field-labels); `neutral-4` as a divider border/button-hover-background (`modal.tsx`) and as text (`badge.tsx`); `neutral-5` as body text (`input.tsx`/`datepicker.tsx`) and as a border (`user-row.tsx`'s status-dot cutout) — each a single-pattern or too-heterogeneous a role to name confidently in this pass. Verified the 3 new classes compiled correctly via a direct check of the built CSS (`bg-surface-overlay`/`bg-surface-panel`/`bg-surface-shell`, each resolving to its own `--color-surface-*` custom property). Updated `colors.mdx` (full alias table + explicit list of what's still un-aliased and why) and `README.md`. `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run build:storybook` all pass with zero visual change.
